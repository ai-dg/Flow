# Required — your Anthropic API key (local demo only; exposed to the browser)
VITE_ANTHROPIC_API_KEY=sk-ant-...

# Required for cloud voice — your ElevenLabs API key (exposed to the browser).
# Without it, TTS falls back to the native speechSynthesis voice.
VITE_ELEVENLABS_API_KEY=sk_...
